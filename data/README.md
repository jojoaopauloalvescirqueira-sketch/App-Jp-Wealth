# Dados

- `backups/`: backups reais locais; ignorados pelo Git.
- `samples/`: fixtures anonimizadas e mínimas para testes.

Nunca salvar neste repositório senha master, senha de investidor, login real, token, chave de API ou backup não anonimizado.
